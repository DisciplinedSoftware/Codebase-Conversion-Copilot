[package]
name = "rust_blas"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_blas"
crate-type = ["lib"]

[features]
default = []

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "blas_bench"
harness = false

[build-dependencies]
